 export const sum = (a,b) => a+b;

export const multy = (a, b) => a*b;

export const div = (a,b) => a/b;

export const sub = (a, b) => a-b;

export const pow = (a,b) => a**b;

const value = 23;

export default value;

//export default {sum, multy, div, sub, pow}